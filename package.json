
{
  "name": "ubaya-student-hub",
  "version": "1.0.0",
  "description": "Student Portal for Native Mobile Programming",
  "main": "index.tsx",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:init": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "build:android": "npm run build && npx cap copy android && npx cap sync android"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "@google/genai": "^1.34.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "vite": "^4.0.0",
    "typescript": "^4.9.0"
  }
}
